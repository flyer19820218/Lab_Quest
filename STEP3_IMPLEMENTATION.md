# 第 3 步實作與驗收紀錄

日期：2026-09-19

## 依據與範圍

完整閱讀 `PROJECT_PLAN.md` 與 `STATIC_ELECTRICITY_LAB_SPEC.md`；以規格卡的直式單一工作檯優先於較早計畫的角色走路。角色依老師後續確認與 v3 圖改為原創國中生。本次實作任務一、二的可玩核心及任務三的純函式科學案例；未修改 `Physical-Boys`。

## 科學引擎

`createState` 建立初始狀態；`reduce` 是不修改前一狀態的純函式。只有接地時能交換電子；驗電器與地面的電荷合計恆為零。相對電荷與金箔角度由電子數推導，renderer 無權自行改寫電荷。正電荷綁在導體上，不會沿金屬桿或導線移動。

任務二同時檢查操作證據與概念回答。錯序不扣分；可直接重試，或以「重新放好器材」重設當題而保留既有本機獎勵。

## 已執行

- `node --test tests/engine.test.cjs`：15 / 15 通過。
- 含四條原始科學驗收：近棒總電荷 0；先斷地後移棒留下正電；先移棒回到中性；等大球 -6 與 0 接觸後 -3、-3。
- 3,000 次操作序列逐步核對守恆、區域電子總數、金箔對稱及孤立狀態淨電荷不變。
- `node --check src/app.js`、`node --check src/engine.js`：通過。
- `tests/browser.test.cjs`：Chrome 桌面 1365px 與 iPhone 13 390px 觸控模擬通過；沒有未捕捉的瀏覽器錯誤、沒有水平溢位。拖曳、按住／放開接地、單指模式、兩任務回答及重載存檔皆通過。
- 同一 SVG 間隔約一秒的像素差異：感應 5,087 像素、接地 4,741 像素，確認電子與金箔有實際運動。截圖：`artifacts/desktop-positive.png`、`artifacts/mobile-positive.png`。

## 驗收邊界

採單一可取消的 requestAnimationFrame，不在切換任務時疊加動畫。器材結構與所有即時數字來自同一引擎。動態器材應在操作後比較畫面；閒置後停止動畫是正常設計，不表示故障。

瀏覽器驗收腳本另覆蓋：直接 file 開啟、暱稱、本機存檔、錯答重試、按住接地、單指模式、手機拖曳、不橫向溢位、金屬桿止於 y=303 而底座在 y=445，以及間隔約一秒的動畫像素差異。

真實 iPhone Safari 尚未驗證；本階段沒有宣稱完成所有手機相容性或整個 P1。任務三、3D 與服裝外觀套用仍是後續範圍。
