# March 角色動畫 v2

日期：2026-09-20

## 操作與角色

- March 的黑髮、方框眼鏡、深藍外套、原子圖案 T 恤，以真正 3D 網格製作。
- 肩 → 肘 → 腕、髖 → 膝 → 踝各自轉動；走路交替屈膝，手臂和同側腿反向擺動。
- 拿起器材後，負電棒附著在右手掌；上臂／前臂長度固定為 0.81／0.79 場景單位。
- 遠／中／近三個位置平滑切換，也能拖曳負電棒選段；接近時仍有空氣間隔。
- 左手伸向接地端，達到接觸位置才導通；右手保持原本持棒位置。再次點擊抬手斷地。
- 手機橫式把提示放到場景上方、操作鈕放到旁邊，同時看見動作與操作鈕；16:9 場景及 1080px 主框架保留。

## 檔案與回退

`index.html`、`lab_3d_walk_preview_0919_v2.html` 使用 `src/game-v2.js` 與 `game-v2.css`。
v1 的 HTML、JS、CSS 完整保留；`src/engine.js` 與原 Physical-Boys 專案未修改。
兩版沿用同一個本機進度鍵，因此在相同網站來源中保留既有經驗與獎勵。

## 驗收

- 15 項科學引擎測試。
- 鍵盤／觸控移動、桌子碰撞、自動繞桌、膝部與擺臂動作。
- 三段手掌位置不同、上臂／前臂骨長固定、握點與手掌一致、棒與圓盤有間隔。
- 左手接觸接地端、右手維持位置、正確／錯誤接地順序、升級與存檔。
- 拿棒與接地動畫前後約一秒的像素差。
- 844×390、667×375 手機橫式：提示不遮場景、主要操作鈕與場景同時可見、無水平溢位。

測試結果和截圖由 `tests/game.test.cjs` 寫入 `artifacts/game-3d-v2/`；該資料夾不提交 Git。

2026-09-20 收尾實測全部通過，瀏覽器錯誤 0。遠／中／近棒與圓盤的最短空隙約為 0.859／0.333／0.122 場景單位，手掌每段位移約 0.655；握點誤差 0。約一秒前後的拿棒／接地像素差為 39,364／2,780 像素。這些數字是遊戲場景幾何與渲染驗收值，不代表實際公尺或電量。

## 參考圖

圖片由內建 ImageGen 依既有 March 正式立姿生成，保存在 `assets/characters/march-reference/`。生成圖僅供角色造型與動作參考；接地導通路徑及棒／圓盤間隙以遊戲幾何與既有科學引擎為準。

- `march-turnaround-v1.png`：正面、四分之三、側面、後方。
- `march-walk-cycle-v1.png`：走路姿勢參考。
- `march-rod-distance-v1.png`：右手持棒遠／中／近。
- `march-grounding-v1.png`：左手伸向接地端。

生成提示詞見同目錄 `PROMPTS.md`。人物仍是輕量程序建模，並非參考圖自動轉成高精度 3D 資產。真實 iPhone Safari 尚未驗證。
